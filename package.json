{
  "name": "mobile-translator",
  "version": "1.0.0",
  "description": "Safari iOS browser extension for bilingual translation — fully open source, free, configurable LLM APIs",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist mobile-translator.zip"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
