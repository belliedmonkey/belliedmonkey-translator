# 1.7.16 发布说明（国际版 11 份 + 中国版 1 份 · iOS 与 macOS 同文）

> 上一个提审的是 **1.7.14**（撤审换成这一版），所以这份覆盖 1.7.14 → 1.7.16。
>
> 三件用户看得见的事：① 登录页重做，一键登录在前；② 每个面都有了「反馈」和「评分」的
> 入口；③ **匿名用量数据**开始发送 —— 这一条必须写进说明，且要说清不含什么、怎么关。
>
> 中国版不同：中国版**不发送任何用量数据**，说明里不能提它；登录页与反馈入口两条照写。
> ⚠️ 不写内部说法（不提 Gate D、不提 MTTelemetry）。

---

## 国际版 · zh-Hans

```
登录页重做了：Apple 和 Google 一键登录放在最上面，邮箱登录折叠成一行，需要时再展开。
验证码那一步不再往下堆，整块换成输入框。

每个面都有了「反馈」和「评分」入口：设置页、弹窗、引导结尾和 App 设置里都能一键写信
给开发者，或去商店评分。

开始发送匿名用量数据：只有用了哪些功能、在哪个浏览器、翻译成功还是失败 —— 不含你读的
网页、文字、地址、密钥或账号。默认开启，设置里一个开关可关，关掉即删除这台设备发过的
数据。隐私政策第 5 节逐条写明。
```

## 国际版 · en-US

```
Sign-in, redesigned: Apple and Google one-tap sign-in sit at the top; email is folded into
a single line and expands only when you need it. The code step now replaces the form
instead of stacking under it.

Feedback and rating are one tap away on every surface — settings, the popup, the end of
onboarding, and the app's settings — to write to the developer or rate on the store.

Anonymous usage data is now sent: only which features are used, on which browser, and
whether a translation succeeded or failed — never the pages you read, the text, the
addresses, your keys or your account. On by default, one switch in Settings turns it off,
and turning it off deletes what this device sent. Privacy policy section 5 lists every field.
```

## 中国版 · zh-Hans

```
登录页重做了：Apple 一键登录放在最上面，邮箱登录折叠成一行，需要时再展开。验证码那一步
不再往下堆，整块换成输入框。

每个面都有了「反馈」和「评分」入口：设置页和 App 设置里都能一键写信给开发者，或去商店
评分。
```

## 国际版 · de-DE

```
Anmeldung neu gestaltet: Apple und Google stehen ganz oben, E-Mail ist zu einer Zeile
zusammengeklappt und öffnet sich nur bei Bedarf. Der Code-Schritt ersetzt das Formular,
statt sich darunter zu stapeln.

Feedback und Bewertung sind überall einen Tipp entfernt – Einstellungen, Popup, Ende der
Einführung und App-Einstellungen.

Anonyme Nutzungsdaten werden jetzt gesendet: nur welche Funktionen genutzt werden, in
welchem Browser, und ob eine Übersetzung gelang – nie die gelesenen Seiten, der Text, die
Adressen, Ihre Schlüssel oder Ihr Konto. Standardmäßig an, ein Schalter in den
Einstellungen schaltet es ab und löscht, was dieses Gerät gesendet hat. Datenschutz, Abschnitt 5.
```

## 国际版 · zh-Hant

```
登入頁重做了：Apple 和 Google 一鍵登入放在最上面，電子郵件登入摺疊成一行，需要時再展開。
驗證碼那一步不再往下堆，整塊換成輸入框。

每個面都有了「意見回饋」和「評分」入口：設定頁、彈出視窗、引導結尾和 App 設定裡都能一鍵
寫信給開發者，或到商店評分。

開始傳送匿名使用資料：只有用了哪些功能、在哪個瀏覽器、翻譯成功還是失敗 —— 不含你讀的
網頁、文字、網址、金鑰或帳號。預設開啟，設定裡一個開關可關，關掉即刪除這台裝置傳過的
資料。隱私權政策第 5 節逐條寫明。
```

## 国际版 · fr-FR

```
Connexion repensée : Apple et Google en un geste tout en haut ; l'e-mail est replié sur une
ligne et ne s'ouvre qu'au besoin. L'étape du code remplace le formulaire au lieu de
s'empiler dessous.

Retour et notation sont à portée de main partout : réglages, fenêtre, fin de l'introduction
et réglages de l'app.

Des données d'usage anonymes sont désormais envoyées : seulement quelles fonctions sont
utilisées, sur quel navigateur, et si une traduction a réussi — jamais les pages lues, le
texte, les adresses, vos clés ni votre compte. Activé par défaut, un interrupteur dans les
réglages le désactive et supprime ce que cet appareil a envoyé. Politique de confidentialité, section 5.
```

## 国际版 · ru

```
Вход переработан: Apple и Google в одно касание — сверху; электронная почта свёрнута в одну
строку и раскрывается только при необходимости. Шаг с кодом заменяет форму, а не
громоздится под ней.

Обратная связь и оценка — в одно касание на каждом экране: настройки, окно расширения,
конец знакомства и настройки приложения.

Теперь отправляются анонимные данные об использовании: только какие функции используются,
в каком браузере и удался ли перевод — никогда не прочитанные страницы, текст, адреса, ваши
ключи или учётная запись. Включено по умолчанию; переключатель в настройках выключает и
удаляет отправленное этим устройством. Политика конфиденциальности, раздел 5.
```

## 国际版 · ar-SA

```
أُعيد تصميم صفحة تسجيل الدخول: Apple وGoogle بلمسة واحدة في الأعلى، والبريد الإلكتروني مطويّ
في سطر واحد يُفتح عند الحاجة فقط. خطوة الرمز تحلّ محل النموذج بدل أن تتكدّس تحته.

الملاحظات والتقييم على بُعد لمسة في كل مكان: الإعدادات، النافذة المنبثقة، نهاية الجولة
التعريفية، وإعدادات التطبيق.

تُرسل الآن بيانات استخدام مجهولة: فقط أي الميزات تُستخدم، وعلى أي متصفح، وهل نجحت الترجمة —
لا الصفحات التي تقرأها ولا النص ولا العناوين ولا مفاتيحك ولا حسابك أبدًا. مفعّلة افتراضيًا،
ومفتاح في الإعدادات يوقفها ويحذف ما أرسله هذا الجهاز. سياسة الخصوصية، القسم 5.
```

## 国际版 · pt-BR

```
Login redesenhado: Apple e Google em um toque ficam no topo; o e-mail fica recolhido em uma
linha e só abre quando você precisa. O passo do código substitui o formulário em vez de se
empilhar embaixo.

Feedback e avaliação a um toque em toda parte: ajustes, janela da extensão, fim da
introdução e ajustes do app.

Dados de uso anônimos passam a ser enviados: só quais recursos são usados, em qual navegador
e se uma tradução deu certo — nunca as páginas que você lê, o texto, os endereços, suas
chaves ou sua conta. Ligado por padrão; um interruptor nos Ajustes desliga e apaga o que este
aparelho enviou. Política de privacidade, seção 5.
```

## 国际版 · es-ES

```
Inicio de sesión rediseñado: Apple y Google con un toque arriba del todo; el correo queda
plegado en una línea y solo se abre cuando lo necesitas. El paso del código sustituye al
formulario en lugar de apilarse debajo.

Comentarios y valoración a un toque en todas partes: ajustes, ventana de la extensión, final
de la introducción y ajustes de la app.

Ahora se envían datos de uso anónimos: solo qué funciones se usan, en qué navegador y si una
traducción tuvo éxito; nunca las páginas que lees, el texto, las direcciones, tus claves ni tu
cuenta. Activado por defecto; un interruptor en Ajustes lo desactiva y borra lo que envió
este dispositivo. Política de privacidad, sección 5.
```

## 国际版 · ko

```
로그인 화면을 다시 만들었습니다: Apple과 Google 원터치 로그인이 맨 위에 오고, 이메일은 한 줄로
접혀 필요할 때만 펼쳐집니다. 인증 코드 단계는 아래에 쌓이지 않고 폼을 통째로 바꿉니다.

피드백과 평가가 어디서나 한 번에: 설정, 팝업, 안내 마지막 화면, 앱 설정.

익명 사용 데이터를 보내기 시작합니다: 어떤 기능을 어떤 브라우저에서 썼는지, 번역이
성공했는지만 — 읽은 페이지, 본문, 주소, 키, 계정은 절대 포함되지 않습니다. 기본 켜짐,
설정의 스위치로 끌 수 있고 끄면 이 기기가 보낸 데이터가 삭제됩니다. 개인정보 처리방침 5절.
```

## 国际版 · ja

```
サインイン画面を作り直しました。Apple と Google のワンタップサインインが最上段に、
メールは1行に畳まれ、必要なときだけ展開します。コード入力はフォームの下に積まれず、
その場で置き換わります。

フィードバックと評価がどの画面からもワンタップで：設定、ポップアップ、導入の最後、
アプリの設定。

匿名の利用データの送信を始めます。送るのは、どの機能をどのブラウザで使ったか、翻訳が
成功したかだけ。読んだページ・本文・URL・キー・アカウントは決して含みません。初期状態で
オン、設定のスイッチでオフにでき、オフにするとこの端末が送ったデータは削除されます。
プライバシーポリシー第5条。
```
