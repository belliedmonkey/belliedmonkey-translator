<!-- 由 store-assets/release-notes-1.6.8.md 派生，供 `node scripts/asc.js notes` 读取。
     工具按「## <国际版|中国版> · <locale>」取块。iOS 与 macOS 文案不同（macOS 有
     「一键跳 Safari 扩展设置」，iPhone 没有；官网那条只对 iPhone 讲得通），所以按平台
     拆成两个文件，而不是让一方读到自己机器上不存在的说明。
     中国版与国际版本轮文案相同：这一版没有任何一方独有的修复。 -->

## 国际版 · zh-Hans

```
第一次打开 App 会有一段引导，带你把翻译真正用起来：选语言、配好翻译引擎、启用
Safari 扩展、然后当场翻一页看看效果。扩展没启用时，App 里可以一键跳到 Safari 的
扩展设置。

· 不登录也能进来看。登录只在你想把复习进度同步到别的设备时才需要。
· 修好了两处会让人白等的地方：翻译如果失败，现在会明确告诉你并给一个可以点的
  重试，而不是一直停在「翻译中」；在设置里填完 API Key 之后直接切走，Key 也不会
  再丢了。
```

## 国际版 · en-US

```
The first launch now walks you through actually getting translation working: pick
your languages, set up an engine, enable the Safari extension, then translate a
page right there to see it work. When the extension is off, the app can jump
straight to Safari's extension settings.

· You can look around without signing in. An account is only needed when you want
  review progress synced to another device.
· Two places that used to leave you waiting are fixed: a failed translation now
  says so and offers a retry you can click, instead of sitting at "Translating…"
  forever; and an API Key you type in Settings is no longer lost if you switch
  away before leaving the field.
```

## 中国版 · zh-Hans

```
第一次打开 App 会有一段引导，带你把翻译真正用起来：选语言、配好翻译引擎、启用
Safari 扩展、然后当场翻一页看看效果。扩展没启用时，App 里可以一键跳到 Safari 的
扩展设置。

· 不登录也能进来看。登录只在你想把复习进度同步到别的设备时才需要。
· 修好了两处会让人白等的地方：翻译如果失败，现在会明确告诉你并给一个可以点的
  重试，而不是一直停在「翻译中」；在设置里填完 API Key 之后直接切走，Key 也不会
  再丢了。
```

## 中国版 · en-US

```
The first launch now walks you through actually getting translation working: pick
your languages, set up an engine, enable the Safari extension, then translate a
page right there to see it work. When the extension is off, the app can jump
straight to Safari's extension settings.

· You can look around without signing in. An account is only needed when you want
  review progress synced to another device.
· Two places that used to leave you waiting are fixed: a failed translation now
  says so and offers a retry you can click, instead of sitting at "Translating…"
  forever; and an API Key you type in Settings is no longer lost if you switch
  away before leaving the field.
```


## 国际版 · ja

```
初回起動時に、翻訳を実際に使えるようにするまでの案内が入りました。言語を選び、翻訳エンジンを設定し、Safari 拡張機能を有効にして、その場で 1 ページ翻訳して確かめられます。拡張機能が無効なときは、App から Safari の拡張機能設定へワンタップで移動できます。

・サインインしなくても中を見られます。アカウントが要るのは、復習の進み具合を別の端末と同期したいときだけです。
・待たされて終わる箇所を 2 つ直しました。翻訳に失敗したときは「翻訳中…」のまま止まらず、失敗したことと押せる再試行を示します。設定で API キーを入力したあと、すぐ別のアプリに移ってもキーが消えなくなりました。
```

## 国际版 · ko

```
처음 실행할 때 번역을 실제로 쓸 수 있게 되기까지 안내가 생겼습니다. 언어를 고르고, 번역 엔진을 설정하고, Safari 확장 프로그램을 켠 다음, 그 자리에서 한 페이지를 번역해 확인할 수 있습니다. 확장 프로그램이 꺼져 있으면 App에서 Safari의 확장 프로그램 설정으로 바로 이동할 수 있습니다.

· 로그인하지 않아도 둘러볼 수 있습니다. 계정은 복습 진행 상황을 다른 기기와 동기화할 때만 필요합니다.
· 기다리다 끝나던 두 곳을 고쳤습니다. 번역이 실패하면 «번역 중…»에 머물지 않고 실패했다는 사실과 누를 수 있는 재시도를 보여 줍니다. 설정에서 API 키를 입력한 뒤 바로 다른 앱으로 옮겨도 키가 사라지지 않습니다.
```

## 国际版 · zh-Hant

```
第一次打開 App 會有一段引導，帶你把翻譯真正用起來：選語言、配好翻譯引擎、啟用 Safari 擴充功能，然後當場翻一頁看看效果。擴充功能沒啟用時，App 裡可以一鍵跳到 Safari 的擴充功能設定。

· 不登入也能進來看。登入只在你想把複習進度同步到別的裝置時才需要。
· 修好了兩處會讓人白等的地方：翻譯如果失敗，現在會明確告訴你並給一個可以點的重試，而不是一直停在「翻譯中」；在設定裡填完 API 金鑰之後直接切走，金鑰也不會再弄丟了。
```

## 国际版 · de-DE

```
Beim ersten Start führt dich jetzt eine Einführung dorthin, wo die Übersetzung wirklich läuft: Sprachen wählen, eine Engine einrichten, die Safari-Erweiterung aktivieren und gleich eine Seite übersetzen, um es zu sehen. Ist die Erweiterung aus, springt die App direkt zu Safaris Erweiterungseinstellungen.

• Du kannst dich umsehen, ohne dich anzumelden. Ein Konto brauchst du nur, wenn der Lernfortschritt auf einem anderen Gerät auftauchen soll.
• Zwei Stellen, an denen man vergeblich wartete, sind behoben: Eine fehlgeschlagene Übersetzung sagt das jetzt und bietet einen anklickbaren Wiederholen-Knopf, statt für immer bei „Übersetze…“ zu stehen. Und ein in den Einstellungen eingetragener API-Schlüssel geht nicht mehr verloren, wenn du direkt danach wegwechselst.
```

## 国际版 · fr-FR

```
Au premier lancement, un guide vous mène jusqu'à une traduction qui fonctionne vraiment : choisir les langues, configurer un moteur, activer l'extension Safari, puis traduire une page sur place pour le constater. Si l'extension est désactivée, l'app peut ouvrir directement les réglages d'extensions de Safari.

• Vous pouvez regarder sans vous connecter. Un compte ne sert que si vous voulez retrouver votre progression de révision sur un autre appareil.
• Deux endroits où l'on attendait pour rien sont corrigés : une traduction qui échoue le dit désormais et propose un bouton Réessayer, au lieu de rester indéfiniment sur « Traduction… » ; et une clé API saisie dans les Réglages n'est plus perdue si vous changez d'app juste après.
```

## 国际版 · es-ES

```
Al abrir la app por primera vez, una guía te lleva hasta tener la traducción realmente funcionando: elegir idiomas, configurar un motor, activar la extensión de Safari y traducir una página allí mismo para verlo. Si la extensión está desactivada, la app puede saltar directamente a los ajustes de extensiones de Safari.

• Puedes mirar sin iniciar sesión. La cuenta solo hace falta si quieres que tu progreso de repaso aparezca en otro dispositivo.
• Arreglados dos sitios donde se esperaba en vano: una traducción fallida ahora lo dice y ofrece un botón de reintento, en lugar de quedarse para siempre en «Traduciendo…»; y una clave de API escrita en Ajustes ya no se pierde si cambias de app justo después.
```

## 国际版 · ru

```
При первом запуске появилось руководство, которое доводит до по-настоящему работающего перевода: выбрать языки, настроить движок, включить расширение Safari и тут же перевести страницу, чтобы убедиться. Если расширение выключено, из приложения можно сразу перейти к настройкам расширений Safari.

• Можно осмотреться и без входа. Учётная запись нужна только тогда, когда вы хотите видеть прогресс повторений на другом устройстве.
• Исправлены два места, где приходилось ждать впустую: неудавшийся перевод теперь так и говорит и предлагает кнопку повтора, а не висит вечно на «Перевод…»; а ключ API, введённый в настройках, больше не теряется, если сразу переключиться.
```

## 国际版 · pt-BR

```
Na primeira abertura, um guia leva você até a tradução realmente funcionando: escolher os idiomas, configurar um motor, ativar a extensão do Safari e traduzir uma página ali mesmo para ver. Se a extensão estiver desativada, o app pode ir direto para os ajustes de extensões do Safari.

• Dá para olhar sem entrar na conta. A conta só é necessária se você quiser o progresso de revisão em outro aparelho.
• Dois lugares em que se esperava à toa foram corrigidos: uma tradução que falha agora diz isso e oferece um botão de tentar de novo, em vez de ficar para sempre em «Traduzindo…»; e uma chave de API digitada nos Ajustes não se perde mais se você trocar de app logo depois.
```

## 国际版 · ar-SA

```
عند التشغيل الأول صار هناك دليل يوصلك إلى ترجمة تعمل فعلاً: اختر اللغات، اضبط المحرّك، فعّل امتداد Safari، ثم ترجم صفحة في الحال لتراه يعمل. وإن كان الامتداد مُعطّلاً، يمكن للتطبيق الانتقال مباشرةً إلى إعدادات امتدادات Safari.

• يمكنك التصفّح دون تسجيل الدخول. الحساب لازم فقط إن أردت أن يظهر تقدّم المراجعة على جهاز آخر.
• أُصلح موضعان كان الانتظار فيهما بلا طائل: الترجمة الفاشلة تقول ذلك الآن وتعرض زر إعادة محاولة، بدل البقاء إلى الأبد عند «جارٍ الترجمة…»؛ ومفتاح الـ API الذي تُدخله في الإعدادات لم يعد يضيع إن انتقلت إلى تطبيق آخر بعده مباشرة.
```
