# 1.7.17 发布说明（国际版 11 份 + 中国版 1 份 · iOS 与 macOS 同文）

> 只为 App 的一个 bug 发的补丁版：App 设置里的「一键配置」，粘贴 API Key 之后点「配好翻译、
> 朗读、转写」，卡片被整个重画，key 和自检那几行一起消失 —— 看起来像密码被吃掉了。
> 1.7.14 带入，1.7.16 已上架（国际版 macOS、中国版 iOS/macOS）。扩展那一侧没有这个问题。
> 说明只写用户看得见的：一句修复 + 一句 1.7.16 的要点回顾（上架说明会被这份覆盖）。

---

## 国际版 · zh-Hans

```
修复：App 设置里的「一键配置」，粘贴 API Key 后点「配好」按钮，key 和自检结果会一起消失。
现在 key 留在原地，三项自检逐行显示结果。

1.7.16 的改动仍在：登录页重做（Apple / Google 一键登录在前）、每个面都有反馈与评分入口、
匿名用量数据（设置里一个开关可关）。
```

## 国际版 · en-US

```
Fix: in the app's Settings, "One key sets up all three" lost the API key — and the three
test rows — the moment you tapped the button. The key now stays put and each test reports
on its own line.

Everything from 1.7.16 is still here: the redesigned sign-in (Apple / Google first),
feedback and rating entry points on every surface, and anonymous usage data with a single
switch in Settings to turn it off.
```

## 中国版 · zh-Hans

```
修复：App 设置里的「一键配置」，粘贴 API Key 后点「配好」按钮，key 和自检结果会一起消失。
现在 key 留在原地，三项自检逐行显示结果。

1.7.16 的改动仍在：登录页重做（Apple 一键登录在前）、每个面都有反馈与评分入口。
```

## 国际版 · de-DE

```
Behoben: In den App-Einstellungen verschwand bei „Ein Schlüssel richtet alle drei ein“ der
API-Schlüssel samt Testzeilen, sobald man auf die Schaltfläche tippte. Der Schlüssel bleibt
jetzt stehen, jeder Test meldet sich in seiner eigenen Zeile.

Alles aus 1.7.16 ist weiterhin da: die neue Anmeldeseite (Apple / Google zuerst), Feedback-
und Bewertungs-Einstiege auf jeder Oberfläche sowie anonyme Nutzungsdaten mit einem Schalter
in den Einstellungen.
```

## 国际版 · zh-Hant

```
修復：App 設定裡的「一鍵設定」，貼上 API Key 後點「設定好」按鈕，key 和自檢結果會一起消失。
現在 key 留在原地，三項自檢逐行顯示結果。

1.7.16 的改動仍在：登入頁重做（Apple / Google 一鍵登入在前）、每個介面都有回饋與評分入口、
匿名使用資料（設定裡一個開關可關）。
```

## 国际版 · fr-FR

```
Correctif : dans les réglages de l'app, « Une clé configure les trois » perdait la clé API
et les trois lignes de test dès que vous touchiez le bouton. La clé reste maintenant en
place et chaque test s'affiche sur sa propre ligne.

Tout ce qui est arrivé en 1.7.16 est toujours là : la connexion repensée (Apple / Google
d'abord), les entrées « avis » et « noter » sur chaque surface, et les données d'usage
anonymes avec un seul interrupteur dans les réglages.
```

## 国际版 · ru

```
Исправлено: в настройках приложения «Один ключ настраивает всё» терял API-ключ и три
строки проверки сразу после нажатия кнопки. Теперь ключ остаётся на месте, а каждая
проверка выводит результат в своей строке.

Всё из 1.7.16 на месте: обновлённый вход (Apple / Google сверху), кнопки отзыва и оценки
на каждом экране и анонимная статистика использования с одним переключателем в настройках.
```

## 国际版 · ar-SA

```
إصلاح: في إعدادات التطبيق، كان «مفتاح واحد يضبط الثلاثة» يُفقد مفتاح API وأسطر الاختبار
الثلاثة فور الضغط على الزر. الآن يبقى المفتاح مكانه ويعرض كل اختبار نتيجته في سطره.

كل ما جاء في 1.7.16 ما زال موجودًا: صفحة تسجيل الدخول الجديدة (Apple / Google أولًا)،
ومداخل التعليقات والتقييم في كل واجهة، وبيانات الاستخدام المجهولة مع مفتاح واحد لإيقافها.
```

## 国际版 · pt-BR

```
Correção: nos ajustes do app, "Uma chave configura os três" perdia a chave de API e as
três linhas de teste assim que você tocava no botão. Agora a chave fica no lugar e cada
teste mostra o resultado na própria linha.

Tudo do 1.7.16 continua: o login redesenhado (Apple / Google primeiro), entradas de
feedback e avaliação em todas as telas e dados de uso anônimos com um único interruptor
nos ajustes.
```

## 国际版 · es-ES

```
Corregido: en los ajustes de la app, «Una clave configura las tres» perdía la clave API y
las tres filas de prueba en cuanto tocabas el botón. Ahora la clave se queda donde está y
cada prueba muestra su resultado en su propia línea.

Todo lo de 1.7.16 sigue aquí: el inicio de sesión rediseñado (Apple / Google primero),
accesos a comentarios y valoración en cada pantalla y datos de uso anónimos con un solo
interruptor en los ajustes.
```

## 国际版 · ko

```
수정: 앱 설정의 「키 하나로 세 가지 설정」에서 버튼을 누르는 순간 API 키와 테스트 세 줄이
함께 사라지던 문제. 이제 키는 그대로 남고 각 테스트가 자기 줄에 결과를 표시합니다.

1.7.16의 변경은 그대로입니다: 새 로그인 화면(Apple / Google 우선), 모든 화면의 피드백·평가
입구, 설정의 스위치 하나로 끌 수 있는 익명 사용 데이터.
```

## 国际版 · ja

```
修正：アプリ設定の「キー1つで3つを設定」で、ボタンを押した瞬間に API キーとテスト3行が
消えていました。キーはそのまま残り、各テストがそれぞれの行に結果を表示します。

1.7.16 の変更はそのままです：新しいサインイン画面（Apple / Google を先頭に）、各画面の
フィードバック・評価の入口、設定のスイッチ1つで止められる匿名の利用データ。
```
