dist/
dist-firefox/
safari-project/
safari-project-macos/
# Legacy output of the removed china flavor (see docs/domain-design.md §7.1).
# Nothing regenerates these; delete them whenever you like — kept ignored only so a
# stale local copy can't be committed by accident.
dist-china/
safari-project-china/
*.zip
*.xpi
node_modules/
.gstack/
pnpm-lock.yaml

# macOS
.DS_Store

# layout regression suite output (screenshots + results.json)
test/layout/artifacts/
test/layout/artifacts-*/

# Claude Code: share the project settings, keep per-machine state out
.claude/settings.local.json
.claude/worktrees/

# Local secrets — provider API keys for verification runs live in the shell
# environment (MT_API_KEY), never in the repo.
.env
.env.*
